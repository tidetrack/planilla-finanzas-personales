# Fuente única de targets de deploy (equivalente a clients/*.yaml de planilla-pymes).
# Creado en la Fase 0 del arnés (docs/permanente/ARNES_TIDETRACK.md, sección 2, paso 4).
# Regla: todo deploy a Apps Script parte de este archivo; nunca clasp push a ciegas.
# El sheet_id fue confirmado por triple fuente el 2026-08-12:
#   1. docs/permanente/MAPA_HOJAS.md (URL de la planilla)
#   2. docs/permanente/TIDETRACK_ARQUITECTURA_ESTRICTA.json (campo id_planilla del scanner)
#   3. Google Drive API (metadata del archivo: titulo y owner verificados)
targets:
  - nombre: personal
    estado: activo
    script_id: "1LaZJcIVLlxWsOOuN3VCBGlb-VyAnshMlTuY3iTdQ_gBjzcD3hSjIldeX"
    sheet_id: "1YXnN-9X1itjpuxOBBwGwH3LSMqeGBtyCrUcFK4xCcUI"
    sheet_titulo: "PLANILLA FINANZAS_v4 .WIP | Personal"
    sheet_owner: "start.tidetrack@gmail.com"
    # Verificado por drift-check contra el remoto el 2026-08-21 (post-deploy: "sin drift").
    # El valor anterior, "0.23.5", estaba stale: el remoto ya venia en 0.38.3 -- las corridas
    # v0.24-v0.38.3 se deployaron sin actualizar este campo. La verdad del estado es el
    # drift-check, no este archivo; se corrige para que vuelvan a coincidir.
    # 2026-08-25 15:19: deploy v0.50.0 con --verificado (el remoto era f820f2a, un commit
    # conocido del repo). Drift-check posterior: "sin drift". Este numero se escribe DESPUES
    # del deploy y verificado contra el remoto, nunca antes: escribirlo por adelantado fue
    # lo que dejo el campo stale en "0.23.5" mientras la planilla corria 0.38.3.
    # 2026-08-25: deploy v0.52.0 -- el merge que restaura los tres modulos de Presupuesto
    # que el deploy de v0.50.0 habia sacado de produccion, mas el rediseno del shell.
    # 2026-08-25 16:44: deploy v0.51.2 con --verificado desde fix/tablero-pendientes. El
    # remoto era un commit conocido del repo (v0.52.1, punta de fix/abm-desplegable-entidad)
    # pero NO ancestro del commit que se pusheaba: --verificado en ese momento solo exigia
    # "conocido", no "ancestro", asi que empujo por encima y la planilla perdio v0.50.0
    # (rediseno del Centro de Operaciones) y v0.52.1 (ajuste de animacion). Nada se perdio de
    # git -- todo seguia en origin/fix/abm-desplegable-entidad -- pero la planilla SI quedo en
    # 0.51.2. Este campo declara lo que HAY, no lo que se quiere: por eso queda en "0.51.2" y
    # no en "0.52.1", aunque el merge de este mismo commit ya trae v0.52.1 al repo. La proxima
    # corrida de sync_targets.command corrige la planilla y recien ahi este campo sube.
    # El fix real de esta cicatriz es devtools/verificar_remoto.py exigiendo ancestria, no solo
    # "commit conocido" (ver c1f5931, incorporado en este mismo merge).
    version_desplegada: "0.55.1"
